import { FilterDeployedPipe } from './filter-deployed.pipe';

describe('FilterDeployedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDeployedPipe();
    expect(pipe).toBeTruthy();
  });
});
