import { FilterAnnexedPipe } from './filter-annexed.pipe';

describe('FilterAnnexedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnexedPipe();
    expect(pipe).toBeTruthy();
  });
});
