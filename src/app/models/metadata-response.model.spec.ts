import { MetadataResponse } from './metadata-response.model';

describe('MetadataResponse', () => {
  it('should create an instance', () => {
    expect(new MetadataResponse()).toBeTruthy();
  });
});
